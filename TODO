level 3

write test fuzzer

Make parse/match/parse* (anything which doesn't use self.obj) static
    or perhaps remove class and make purely functional.
more unit tests
implement T.empty
write better README. format in RST
    mention what it doesn't implement
